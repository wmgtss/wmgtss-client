import LoginForm from './LoginForm/LoginForm';

/**
 * Renders the login form. This is completely redundant
 * but I'm sure something will break if I remove it
 */
export default function LoginPage() {
    return <LoginForm />;
}
